{
  "name": "ai-transcription-worker",
  "version": "0.1.0",
  "private": true,
  "description": "AI Worker (Node wrapper + Python subprocess) cho offline local meeting transcription pipeline (M1 — faster-whisper).",
  "main": "dist/transcription-job-runner.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "test": "jest",
    "job-runner": "node dist/transcription-job-runner.js"
  },
  "dependencies": {
    "minio": "^8.0.7"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^22.10.0",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.2"
  }
}
